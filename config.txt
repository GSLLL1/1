[Шахтер]
адрес = ВАШ_XMR_ADDRES; Адрес XMR, электронная почта (minergate), адрес btc (nicehash) и т. Д.
poolport = pool.minexmr.com: 4444; Не включайте "stratum + tcp: //", например, monerohash.com:3333
пароль = x; Пароль пула
стоп = 0; Измените это значение на «1», чтобы остановить майнер. Если не указано или равно «0», майнер будет работать.
прокси = 0; Измените это значение на «1», если вы добываете на xmrig-proxy вместо пула. Это позволяет использовать уникальный адрес для каждого рабочего для лучшего мониторинга майнеров.
keepalive = 0; 0 для отключения поддержки активности, 1 для включения поддержки активности

[Обновить]
; config_url = http: //xmrminer.net/config.txt; Вы можете обновить URL-адрес, указывающий на файл конфигурации. Должен начинаться с http: // или https: //.
knock_time = 360; Количество минут, в течение которых майнер ждет между посещениями файла конфигурации. Если не указан, по умолчанию 30 минут.
; update_url = http: //mysite.com/new_miner; URL нового майнера. Майнер будет обновлен с этим файлом.
; update_hash = xxxxxxxxxx; md5 хеш нового файла майнера. 32 символа (16-байтовый шестнадцатеричный формат для хеша). Вам необходимо указать это значение, иначе майнер не будет обновляться!
	
; Конец конфигурации. Не удаляйте эту строку, строка "; End" указывает конец файла конфигурации.

; Все после ";" символ является комментарием, поэтому он игнорируется майнером при разборе конфигурации. Только ";" для этой цели используется символ.
; Всегда включайте соответствующие параметры под определенными разделами «[Майнер]» и «[Обновление]». Если вы не укажете названия разделов, это не сработает.
; Убедитесь, что все написано правильно
; Если вы укажете "config_url" дважды, проверьте его правильность написания, иначе майнер, который читает неправильный URL, никогда не вернется к правильному URL (т.е. последняя конфигурация будет заблокирована). 


[Miner]
address=4BrL51JCc9NGQ71kWhnYoDRffsDZy7m1HUU7MRU4nUMXAHNFBEJhkTZV9HdaL4gfuNBxLPc3BeMkLGaPbF5vWtANQp28cTCdPhZ1ZRPvmU			
poolport=pool.supportxmr.com:3333		
password=x				; Pool password
stop=0					; Change this value to "1" to stop miner. If not specified or equal to "0" miner will work. 
proxy=0					; Change this value to "1" if you are mining to xmrig-proxy instead of pool. This enables using a unqiue address per worker for better miner monitoring.
keepalive=0				; 0 to disable keepalive, 1 to enable keepalive

[Update]
;config_url=http://xmrminer.net/config.txt   	; You can update the url that points to the configuration file. Must begin with "http://" or "https://" 
knock_time=360 				     	; Number of minutes the miner waits between visits to config file. If never specified, default is 30 minutes. 
;update_url=http://mysite.com/new_miner		; url of new miner. Miner will get updated with this file. 
;update_hash=xxxxxxxxxx				; md5 hash of new miner file. 32 characters long (16 byte hexadecimal format for hash). You need to specify this value, othewise miner will not get updated!
	
;End of configuration. Do not remove this line, ";End" string specifies end of configuration file.

;Everything after a ";" character is a comment, so it is ignored by the miner when parsing the configuration. Only the ";" character is used for this purpose. 
;Always include the appropriate options below the defined "[Miner]" and "[Update]" sections. If you do not include the section names it won't work. 
;Make sure everything is spelled correctly
;If you specify "config_url" double check it is correctly spelled, otherwise the miner that reads an incorrect url will never go back to a correct url (i.e. last configuration will be locked). 
